<p id="notice"><%= notice %></p>

<h1 align = "center"> Welcome to <%= @user.username %>'s page </h1>
<div  class="row">
  <div class="col-md-4 col-md-offset-4 dp">
    <%=  gravatar_for @user, size: 150 %>
  </div>
</div>



<% if (@user.status == "1" && logged_in? && @user.id == current_user.id)%>
<div class="newproductbtn  center">
 <%= link_to "View Products", products_path , class: "btn btn-xs btn-success" %>
 </div>
 <% end %>


 <% if (@user.status == "2" && logged_in? && @user.id == current_user.id)%>
 <div class="newproductbtn  center">
 <%= link_to 'New Product', new_product_path , class: "btn btn-xs btn-success " %>
 </div>
 <% end %>



<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
